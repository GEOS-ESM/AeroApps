#!/bin/csh -f
#
# This script runs the simple gigatraj model, gt_model_s01,
# to trace parcel trajectories kinematically
# for 3 days from yesterday.
#
# The met data source used is GEOS FP, both assimilation (for the past)
# and forecast (for the future). Note that we read the data from
# the OPeNDAP server, which is slow and can be rather flakey.
# If the model run fails, complaining about being unable to read data
# from the server, then wait a bit and try again later.
#
# The parcel initial positions were generated by demo_gen_disc_text.sh.
#

# Call this with
# ./run_kinematic.csh string start stop
# where string is used to parse in and out filenames and
# start and stop are YYYY-MM-DDTHH:MM:SS

# The gigatraj model is installed in $ROOT

# We start the parcels off at Yesterday's date, at 13:45 GMT
set Start = $2
# and we trace them forward until tomorrow at 20:00 GMT
set Stop  = $3

# we will have the internal starting model time (t=0)
# be the same as the parcel starting toime
set ZeroTime = $2

# the file of input parcel initial positions
set string = $1
set InFile="parcel_init.$string.nc"

# Input vertical coordinate
set InVert = "PAlt"

# the file of output parcel positions
set OutFile = "parcel_traj.$string.nc"
# the output Format:
#  model_Time  calendar_Time parcel_Id  lOngitude  lAtitude  Vertical 
# Note that while the trajectories are calculated using pressure
# as th evertical coordinate, those pressures are converted
# to the input parcel vertical coordinate, pressure altitude,
# before being written to output. (This conversion does slow the 
# model down a little.)
set OutFormat = "%t %T %i %o %a %v %{PAlt}m"


# the meteorological data source to use
set MetSrc = "MetGEOSfp"
# Since we are running trajectories into the future, we need
# to supply a GEOS FP model run. Choose today's 00Z run
set MetSpec = "ModelRun=`date +"%Y%m%d"`_00"
# the vertical coordinate to use in tracing the trajectories 
# (pressure; these will be kinmatic trajectories)
set VTrace = "P"
# The model time step, in minutes
set TStep = 15
# The model-time frequency in which the output is written, in hours
set OFreq = "1.0"

# get the current time (so we can see how log it takes to run)
set T0 = `date +'%s'`

# run the model
module list
echo $BASEDIR
echo $ROOT
${ROOT}/bin/gtmodel_s01 \
         --verbose \
         --begdate "${Start}" \
         --enddate "${Stop}" \
         --zerodate "${ZeroTime}" \
         --tstep ${TStep} \
         --source "${MetSrc}" \
         --metoptions "${MetSpec}" \
         --vertical "${VTrace}" \
         --parcels "${InFile}" \
         --parcelvertical "${InVert}" \
         --input_netcdf \
         --frequency ${OFreq} \
         --netcdf_out "${OutFile}" \
         --format "${OutFormat}"

# get the time now
set T1 = `date +'%s'`
# how long did this take to run?
set Elapsed = `expr ${T1} - ${T0}`
echo "Time to run: ${Elapsed} seconds"
